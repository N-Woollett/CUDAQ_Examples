# Hello World

## Setup

To run this example, you need to create and activate the Conda environment using the provided `environment.yml` file.

1. **Create the environment:**
   ```bash
   conda env create -f environment.yml
   ```

2. **Activate the environment:**
   ```bash
   conda activate cudaq-env
   ```
